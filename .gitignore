# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class

# Distribution / packaging
.Python
build/
dist/
*.egg-info/

# Unit test / coverage reports
.coverage
.coverage.*
.cache
.pytest_cache/

# Environments
.env
.venv
env/
venv/
ENV/

# IDE
.vscode/
.idea/

# AWS / SSH secrets — never commit these
*.pem
*.ppk

# Personal reference material with real credentials/account IDs/IPs —
# screenshots taken while working through this assignment, AWS access keys,
# and other secrets. Never push this folder, even though it lives inside
# the repo for convenience. See docs/Learning_Journal for the sanitized,
# public-safe version of this same material.
PRIVATE_do_not_share/
